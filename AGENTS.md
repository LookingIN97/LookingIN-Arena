# Project Instructions

- 使用中文沟通，结论直接、具体。
- 本仓库只保存 LookingIN Arena 的公开静态站点与治理文档；比赛计算和导出实现属于相邻的 `LookingIN-Source` 仓库。
- `docs/` 是 GitHub Pages 唯一发布目录。不得提交原始构筑 PNG、分享码、`LookingIN.PlayerBuildShare` / `LKI-PVP1` 元数据、完整构筑摘要、输入绝对路径、私有源码或发布制品。
- `docs/patches/<steam-build-id>/` 中状态为 `final` 的历史页面不可改写。当前 Build 可由 Source 中的显式 Arena 发布工具更新；出现新 Build 后自动冻结上一 Build。
- 常规榜单更新必须由 Source 的显式发布工具生成、验证、提交并推送；不要手工拼装榜单或混用不同比赛运行的文件。
- 每个内聚变更单独提交。除用户明确要求外，不创建 Release、tag、ZIP 或其他部署渠道。
